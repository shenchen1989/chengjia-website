
// import { GoogleGenAI } from "@google/genai";
// import { Language } from "../types";

// Service temporarily disabled to fix build errors with unused files.
// Will be re-enabled when AI features are needed.

export const generateDesignAdvice = async (
  prompt: string,
  history: { role: string; text: string }[],
  lang: 'zh' | 'en'
): Promise<string> => {
    return "";
};
